

## 建议优化后的模块结构

你原来的结构已经很好，可以升级成四区：

---

# 模块四：场景适配器 / 生活流压力测试仪

## 1. 顶部：任务模式选择器

不要叫“场景模式”，可以叫得更有决策感：

```text
当前任务协议：
[ 🟢 考证冲刺 / 防御态 ]
[ 🔵 城市游荡 / 观察态 ]
[ 🟣 社交防御 / 武装态 ]
[ ⚪ 低能量日 / 保命态 ]
```

我建议新增一个很重要的模式：

### ⚪ 低能量日 / 保命态

因为很多时候你不是在备考、city walk 或社交，而是处于：

> 不想被衣服烦、不想被自己烦、不想被世界烦。

这个模式很适合你的系统气质。

它的权重可以是：

```python
"低能量日": {
    "comfort": 1.0,
    "decision_fatigue": 1.0,
    "temperature_control": 0.8,
    "appearance_pressure": -0.5,
    "social_visibility": 0.2
}
```

输出风格可以更温柔但依旧冷静：

> [APPROVED] 这套不会制造额外任务。适合低电量状态下维持基本体面。

---

## 2. 左侧：目标图上传 + 补充信息

左侧不要只放图片上传，建议加一个“现实条件补丁”。

例如：

```text
上传目标图：
[ drag image here ]

今日现实参数：
- 预计外出时长：8小时+
- 是否久坐：是
- 是否大量步行：否
- 是否背包：是
- 当前身体状态：胃胀 / 经期 / 睡眠不足 / 正常
- 今日天气：冷 / 热 / 温差大 / 空调房
```

这会让测试结果更像真的“生活流压力测试”，而不是单纯看图说话。

---

## 3. 右侧 A 区：核心生理约束

建议命名为：

```text
A. 生理约束锁定 Physical Constraint Lock
```

每条规则最好包含三个字段：

```json
{
  "item": "腹部压迫感",
  "status": "ABORT",
  "evidence": "目标图包含高腰紧身牛仔裤，腰腹区域贴合度高",
  "reason": "当前场景需要长时间久坐，腹部压迫会影响呼吸和胃肠舒适度",
  "fix": "替换为松紧腰裤、低压腰头长裤或宽松半裙"
}
```

前端展示可以是：

```text
🔴 ABORT｜腹部压迫感
高腰紧身牛仔裤 → 久坐 8 小时会持续占用身体注意力。
修正：换松紧腰 / 软腰头 / 宽松直筒裤。
```

---

## 4. 右侧 B 区：环境摩擦力评估

这里的进度条要注意方向。

“摩擦力”越高，其实越糟糕；
但你例子里“人群隐蔽性 90%”是越高越好。

建议拆成两类：

### 消耗型指标：越高越危险

```text
步行摩擦：██████░░░░ 60%
穿脱摩擦：████████░░ 80%
清洗维护摩擦：█████░░░░░ 50%
```

### 保护型指标：越高越好

```text
人群隐蔽性：█████████░ 90%
温度适配性：███████░░░ 70%
情绪稳定性：████████░░ 80%
```

这样用户不会混淆“90% 到底是好还是坏”。

---

## 5. 右侧 C 区：最终裁决

这个区域可以保留黑底白字代码框，非常适合做记忆点。

但建议裁决不要只有 `[REJECTED]`，而是分四类：

```text
[APPROVED]
可直接执行。此穿搭不会显著消耗当前任务能量。

[MODIFY]
主体可保留，但必须替换某个高风险部件。

[DELAY]
现在不适合，但可存入未来场景愿望单。

[REJECTED]
与当前身体/任务/环境冲突，不建议购买或穿出门。
```

这样可以避免系统过于暴力。很多衣服不是完全不行，而是“今天不行”。

比如：

```text
[DELAY]
衣服很美，但它服务的是“被观看的下午”，不是“连续做题 8 小时的上午”。
当前处于考证冲刺期，建议存入：未来低任务密度社交场景。
```

这个风格很适合你。

---

## 四、优化后的 Prompt 框架

你可以把系统提示词设计成这样：

```text
你是一个“生活流压力测试仪”，用于评估一套穿搭是否适配用户当前的真实生活场景。

你的目标不是评价穿搭是否好看，而是判断它是否会在特定场景下制造身体不适、行动摩擦、情绪消耗或任务中断。

你必须遵守以下原则：

1. 生理不适优先于审美。
2. 当前任务优先于幻想场景。
3. 舒适度、行动效率、温度调节、身体压力是核心判断项。
4. 如果某件单品会破坏当前场景的主要任务，应直接给出 ABORT。
5. 不要用模糊鼓励语言，不要说“看你喜不喜欢”。你必须给出明确裁决。
6. 允许美，但美不能抵消身体痛苦。
```

### 用户约束字典

```json
{
  "久坐不适": {
    "trigger": ["硬质腰带", "紧身裤", "高腰压腹", "无弹力牛仔"],
    "risk": "腹部压迫、呼吸不畅、胃肠不适、注意力被身体拉走",
    "severity": "high"
  },
  "脚部支撑需求": {
    "trigger": ["薄底鞋", "硬底小皮鞋", "无缓冲平底鞋", "新鞋长时间步行"],
    "risk": "脚底痛、膝盖疲劳、城市游荡半途崩盘",
    "severity": "high"
  },
  "温度敏感": {
    "trigger": ["单件厚衣", "无法叠穿", "闷汗材质", "室内外温差大"],
    "risk": "冷热失控、无法快速调节、烦躁感上升",
    "severity": "medium"
  },
  "低能量保护": {
    "trigger": ["需要频繁整理", "容易走光", "容易皱", "需要端着"],
    "risk": "增加自我监控，消耗精神能量",
    "severity": "medium"
  }
}
```

### 场景参数字典

```json
{
  "exam_sprint": {
    "name": "考证冲刺 / 防御态",
    "core_task": "长时间静坐学习，维持专注和身体稳定",
    "environment": ["空调房", "图书馆", "自习室", "久坐8小时"],
    "hard_constraints": ["腹部压迫", "温度不可调", "频繁整理衣物"],
    "weights": {
      "comfort": 1.0,
      "sitting_compatibility": 1.0,
      "temperature_control": 0.9,
      "aesthetic": 0.1,
      "social_visibility": 0.2
    }
  },
  "city_walk": {
    "name": "城市游荡 / 观察态",
    "core_task": "长距离步行、观察城市、保持身体自由",
    "environment": ["户外", "地铁", "街道", "河边", "山路或长线步道"],
    "hard_constraints": ["鞋底无支撑", "包容量不足", "衣物行动受限"],
    "weights": {
      "shoe_support": 1.0,
      "mobility": 0.9,
      "climate_adaptability": 0.8,
      "low_visibility": 0.8,
      "aesthetic": 0.3
    }
  },
  "social_defense": {
    "name": "社交防御 / 武装态",
    "core_task": "维持边界感、体面感和心理安全区",
    "environment": ["社交场合", "咖啡店", "聚会", "陌生人环境"],
    "hard_constraints": ["过度暴露", "需要持续端着", "行动不自在"],
    "weights": {
      "boundary_protection": 1.0,
      "confidence": 0.8,
      "comfort": 0.7,
      "aesthetic": 0.6,
      "maintenance_cost": 0.8
    }
  },
  "low_energy_day": {
    "name": "低能量日 / 保命态",
    "core_task": "减少决策疲劳和身体噪音",
    "environment": ["通勤", "短途出门", "低社交需求"],
    "hard_constraints": ["任何需要频繁整理的衣服", "任何明显压迫身体的单品"],
    "weights": {
      "comfort": 1.0,
      "ease": 1.0,
      "temperature_control": 0.8,
      "aesthetic": 0.2,
      "social_visibility": 0.5
    }
  }
}
```

### JSON 输出结构

```json
{
  "scenario": "exam_sprint",
  "mode_label": "🟢 考证冲刺 / 防御态",
  "physical_constraints": [
    {
      "name": "腹部压迫感",
      "status": "ABORT",
      "status_icon": "🔴",
      "evidence": "高腰紧身裤，腰腹贴合度高",
      "impact": "长时间久坐时可能压迫腹部，影响呼吸和胃肠舒适度",
      "fix": "替换为松紧腰裤或软腰头长裤"
    }
  ],
  "environmental_friction": [
    {
      "name": "久坐兼容性",
      "type": "risk",
      "percent": 85,
      "label": "高摩擦",
      "reason": "裤腰和面料都不适合长时间坐姿"
    },
    {
      "name": "温度调节灵活性",
      "type": "protection",
      "percent": 40,
      "label": "偏低",
      "reason": "单件厚卫衣不方便应对空调房温差"
    }
  ],
  "verdict": {
    "code": "REJECTED",
    "summary": "衣服很美，但它属于需要时刻端着的衣服。",
    "instruction": "当前处于考证冲刺期，你需要的是能让身体消失在任务里的衣服。建议拔草，或存入未来高能耗社交愿望单。",
    "wishlist_category": "未来高能耗社交"
  }
}
```

---

## 五、Streamlit 页面可以怎么写

你的 `scenario_test.py` 可以先不接真实大模型，先做一个 UI 壳子和假数据，验证交互感觉。

页面结构建议：

```python
import streamlit as st

st.set_page_config(page_title="生活流压力测试仪", layout="wide")

st.title("🚀 生活流压力测试仪")
st.caption("Pre-flight Checklist for Real Life Outfits")

scenario = st.radio(
    "选择当前任务协议",
    [
        "🟢 考证冲刺 / 防御态",
        "🔵 城市游荡 / 观察态",
        "🟣 社交防御 / 武装态",
        "⚪ 低能量日 / 保命态"
    ],
    horizontal=True
)

left, right = st.columns([1, 1.4])

with left:
    st.subheader("目标网图上传区")
    uploaded_file = st.file_uploader("拖拽上传穿搭图片", type=["jpg", "jpeg", "png"])

    st.subheader("今日现实参数")
    duration = st.selectbox("预计外出时长", ["2小时以内", "4小时左右", "8小时以上"])
    sitting = st.toggle("是否长时间久坐")
    walking = st.toggle("是否大量步行")
    body_state = st.multiselect(
        "当前身体状态",
        ["正常", "胃胀", "经期", "睡眠不足", "低能量", "怕冷", "怕热"]
    )

with right:
    st.subheader("A. 生理约束锁定")

    st.error("🔴 ABORT｜腹部压迫感\n\n高腰紧身牛仔裤 → 久坐场景下可能压迫腹部，打断专注。")
    st.warning("🟡 WARNING｜温度调节灵活性\n\n单件厚卫衣 → 空调房忽冷忽热时不方便快速穿脱。")
    st.success("🟢 PASS｜人群隐蔽性\n\n深色低饱和搭配 → 不会引发过度关注。")

    st.subheader("B. 环境摩擦力扫描")

    st.write("步行摩擦：60%")
    st.progress(60)

    st.write("久坐摩擦：85%")
    st.progress(85)

    st.write("人群隐蔽性：90%")
    st.progress(90)

    st.subheader("C. 最终裁决协议")

    st.code(
        """
[REJECTED]

衣服很美，但它属于“需要时刻端着”的衣服。
当前处于考证冲刺期，你需要的是能让身体消失在任务里的衣服。

建议：
- 拔草
- 或存入「未来高能耗社交」愿望单
        """,
        language="text"
    )
```

这个壳子先跑起来，就已经有很强的产品感觉了。

---

## 六、我建议加入的高级功能

### 1. “拔草理由存档”

每次 AI 判定 `[REJECTED]`，都把结果存入一个本地 JSON 或 SQLite。

以后你可以看到：

```text
我为什么没买这件？
- 腹部压迫
- 不适合久坐
- 鞋底太薄
- 只适合幻想场景
```

这会变成你的“消费欲望冷却档案”。

---

### 2. “未来场景愿望单”

不是所有衣服都要被否定。有些衣服只是“不适合今天”。

所以最终裁决可以给出：

```text
存入：
[ ] 考证后社交
[ ] 旅行拍照
[ ] 秋季咖啡馆
[ ] 高能量日
[ ] 不建议存档，直接拔草
```

这比单纯拒绝更人性化，也能降低“我是不是在压抑自己”的反弹心理。

---

### 3. “替代方案生成”

每个红灯项后面都应该给一个替代方向。

例如：

```text
问题：高腰硬质牛仔裤
替代：软腰头直筒裤 / 垂感运动裤 / 宽松半裙

问题：薄底小皮鞋
替代：厚底乐福鞋 / 运动休闲鞋 / 有足弓支撑的短靴

问题：单件厚卫衣
替代：薄打底 + 开衫 / 衬衫外套 / 可脱卸马甲
```

这样工具不只是批判，而是帮你重建可执行方案。

---

## 七、最终评价

这个方案的优点是：

> 它非常适合你这种想把生活变得更理性、更低内耗、更抗消费诱惑的目标。
> 它不是穿搭推荐器，而是“身体与场景的防御系统”。

但需要优化三点：

1. **不要完全依赖图片识别**，要加入用户补充输入。
2. **不要只有拒绝**，要有 `APPROVED / MODIFY / DELAY / REJECTED` 四级裁决。
3. **不要让系统只批判**，每个风险都要附带替代方案。

我会把这个模块的最终定位改成：

> **模块四：场景适配器 / 生活流压力测试仪**
> 一个用于识别穿搭是否会在真实生活中制造身体压力、行动摩擦和情绪内耗的决策系统。它不服务于“看起来像谁”，而服务于“今天的我能不能顺利活下去”。
